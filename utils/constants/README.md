# rorolala-utils-constants

Where Rorolala keeps things: the paths, directory names, environment variables and file
extensions its crates have to agree on.

Nothing here is logic — it is the shared vocabulary of the layout, kept in one place so
that a Workspace, a Vault and the keyring cannot drift apart about where a key lives.
