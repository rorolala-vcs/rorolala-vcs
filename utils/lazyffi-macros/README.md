# rorolala-utils-lazyffi-macros

Procedural macros for FFI export.

This crate is an implementation detail of `rorolala-utils-lazyffi`, which re-exports it.
Depend on `rorolala-utils-lazyffi` instead of this crate.
