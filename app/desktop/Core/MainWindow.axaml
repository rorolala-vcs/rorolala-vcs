<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="1100" d:DesignHeight="720"
        x:Class="RorolalaDesktop.MainWindow"
        Width="1100" Height="720"
        Title="Rorolala Desktop">
    <!-- The menu bar is always present, and the rest of the window is the dock area. Both are filled
         from what the plugins registered, so this file names the shape and the code names the rest. -->
    <DockPanel>
        <Menu x:Name="MainMenu" DockPanel.Dock="Top" />
        <ContentControl x:Name="DockAreaHost" />
    </DockPanel>
</Window>
